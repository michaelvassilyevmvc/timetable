<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Timetable.Views.LearningGroupsPage">
    <ContentPage.Content>
        <StackLayout>
            <SearchBar x:Name="elSearch" CancelButtonColor="Red" Placeholder="Поиск..." 
                       HorizontalTextAlignment="Center" TextChanged="elSearch_TextChanged"
                       ></SearchBar>
            <ListView ItemsSource="{Binding LearningGroups}" x:Name="lstLearningGroups" Margin="20">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding Name}"></Label>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>